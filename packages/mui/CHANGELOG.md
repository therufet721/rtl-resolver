# Changelog

## 0.2.0 — 2026-08-17

First coordinated npm release. Published JS now starts with `"use client"`.

## 0.1.0 — 2026-08-17

MUI `ThemeProvider`, `Dialog`, `Menu`, `Popover`, and `Select` wrappers.
Requires `@mui/material` as a peer. Does not wrap the full MUI catalog.
