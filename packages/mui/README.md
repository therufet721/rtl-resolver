# @rtl-resolver/mui

Wraps MUI `ThemeProvider`, `Dialog`, `Menu`, `Popover`, and `Select` so
`theme.direction` and `dir` follow `@rtl-resolver/react`. Children stay in DOM
order. This package peers `@mui/material` and Emotion; it does not vendor MUI
or wrap the full catalog.
