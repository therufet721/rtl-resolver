# @rtl-resolver/radix

Wraps Radix `DirectionProvider`, dialog, dropdown menu, popover, and select
content so `dir` follows `@rtl-resolver/react`. Children stay in DOM order.
Peer-depends on the corresponding `@radix-ui/react-*` packages. Does not wrap
the full Radix catalog.
