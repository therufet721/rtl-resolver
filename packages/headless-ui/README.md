# @rtl-resolver/headless-ui

Wraps Headless UI dialogs, menus, popovers, and listboxes with `dir` from
`@rtl-resolver/react`. Children stay in DOM order. Peer-depends on
`@headlessui/react`. Does not wrap the full Headless UI catalog.
