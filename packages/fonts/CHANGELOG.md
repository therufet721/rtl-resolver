# Changelog

## 0.2.0 — 2026-08-17

First coordinated npm release.

## 0.1.0 — 2026-08-17

Script coverage, cmap inspection, WOFF/WOFF2 unwrap, GSUB/GPOS flags, and
HarfBuzz WASM shaping. `arabicJoiningFont()` proves isol vs init/fina glyph IDs.
WOFF2 unwrap reconstructs empty/simple `glyf`/`loca` and `hmtx`; composites are
skipped.
